spring:
  application:
    name: auth-server

server:
  port: 8888
  servlet:
    context-path: /library/auth-server

logging:
  level:
    root: INFO
    com.library.authserver: DEBUG
    org.springframework.web: ERROR
